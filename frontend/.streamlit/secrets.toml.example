# Streamlit Cloud secrets — copy this format to:
# App Settings → Secrets in Streamlit Cloud dashboard
# Do NOT commit actual secrets to Git

[general]
BACKEND_URL = "https://your-backend.railway.app"
